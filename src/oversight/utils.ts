/**
 * `utils` entry-point exports the
 * `evidenceDigest` helper for callers who want it without the full
 * client.
 */
export { evidenceDigest } from "./evidence-digest.js"
export { canonicalize } from "../canonicalize.js"
